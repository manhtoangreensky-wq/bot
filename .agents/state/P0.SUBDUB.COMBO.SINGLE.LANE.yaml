version: "2.0"
task_id: "P0.SUBDUB.COMBO.SINGLE.LANE"
repository: "manhtoangreensky-wq/bot"
branch: "fix/p0-subdub-combo-single-lane"
base_sha: "35b6d3c3811d19b39e98e2509922f4fcef6e3b6e"
head_sha: "8d6b9f1b852ca13119a4ca4916bae4517ad8d507"
phase: "VERIFY"
goal: "Expose one SubDub combo lane and deliver exactly one final translated and dubbed MP4."
scope:
  allowed_files:
    - "bot.py"
    - "docs/superpowers/plans/2026-08-21-subdub-combo-single-lane.md"
    - "docs/superpowers/specs/2026-08-21-subdub-combo-single-lane-design.md"
    - "tests/test_p0_*.py"
    - "tests/test_core.py"
  protected_files:
    - "payment and wallet modules"
    - "database schema and ENV"
    - "Video creation and Video Edit engines"
acceptance:
  - "One upload lane: language -> default/automatic voice -> final confirmation."
  - "No public original-subtitle, transcript, SRT, or audio artifact in the fresh combo lane."
  - "Completion requires canonical Telegram MP4 delivery evidence."
  - "MP4-as-document remains available for large files and no message follows the final MP4."
tests:
  - "focused pytest selector: TIMEOUT_AFTER_60S during bot.py import; process terminated"
  - "changed test AST parse: PASS"
  - "static single-lane and delivery comparators: PASS"
  - "git diff --check: PASS"
evidence:
  - "PROVIDER_CALLS=0 before authorized live smoke"
  - "WALLET_MUTATIONS=0 before authorized live smoke"
decisions:
  - "Keep ASR_PROVIDER=auto route order required by existing origin/main contract test."
  - "Keep legacy callbacks registered but hidden for old Telegram messages."
blockers:
  - "GitHub CLI authentication must be refreshed before push/PR."
owner_gates:
  deploy_approved: true
  provider_approved: true
  wallet_live_job_approved: true
next_action: "Refresh GitHub auth, push one PR, merge after CI, verify VPS, then run the authorized two-speaker live job."
updated_at: "2026-08-21T17:20:00+07:00"

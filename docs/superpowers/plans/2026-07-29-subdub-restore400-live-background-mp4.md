# SubDub Restore400 Live Background MP4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore the proven PR400 four-lane executor so a final-confirmed Telegram job runs in the background through ASR, translation, TTS, mux, validation and real MP4 delivery.

**Architecture:** Keep the current UI and the canonical `subtitle_dub_product_pipeline` renderer path. Separate informational public/smoke readiness from final-confirm admission: a confirmed job still requires explicit configured providers and media runtime, but stale smoke, generic freeze and provider-wide public flags do not silently stop it at 5%. Persist enough job truth to restore terminal panels and deliver an already-created MP4 after restart without creating any replacement provider task.

**Tech Stack:** Python, python-telegram-bot, SQLite persistence, FFmpeg/FFprobe, pytest.

---

### Task 1: Reproduce the 5% gate failure

**Files:**
- Create: `tests/test_p0_subdub_restore400_live_background_mp4.py`

- [x] Add a final-confirm gate test with four lane flags off, stale smoke and configured explicit providers.
- [x] Prove the current code blocks before the runner.
- [x] Add negative controls for missing provider configuration and missing final confirmation.

### Task 2: Admit only explicitly confirmed product jobs

**Files:**
- Modify: `bot.py`
- Test: `tests/test_p0_subdub_restore400_live_background_mp4.py`

- [x] Mark the internal state only on the existing `videodub|final` edge.
- [x] Make all SubDub gate layers use one canonical confirmed-product readiness decision.
- [x] Keep provider configuration, explicit provider policy, maintenance and FFmpeg/FFprobe as hard blockers.
- [x] Treat public flags, stale smoke and generic provider freeze as diagnostics for confirmed jobs.
- [x] Keep drafts and previews unable to submit; status recovery may only deliver an already validated, never-attempted MP4.

### Task 3: Carry confirmation to the selected provider adapters

**Files:**
- Modify: `bot.py`
- Test: `tests/test_p0_subdub_restore400_live_background_mp4.py`

- [x] Permit the selected ASR, translation and TTS route only for the confirmed SubDub product path.
- [x] Keep `auto` disallowed and keep automatic paid fallback at zero.
- [x] Verify Key4U MiniMax works when its global public flag is off but the SubDub job was explicitly confirmed.
- [x] Keep cue TTS calls sequential and set the default SubDub speech rate to `0.95`.

### Task 4: Prove four processed MP4 lane contracts offline

**Files:**
- Test: `tests/test_p0_subdub_restore400_live_background_mp4.py`

- [x] Run all four modes through the canonical blackbox/product runner with fixture ASR, translation, TTS and render seams.
- [x] Assert mode-specific stages execute and output bytes are generated by render, not copied source bytes.
- [x] Assert one final MP4 per lane, no provider fallback, no wallet mutation and no Telegram delivery in offline tests.

### Task 5: Restore safe persisted terminal handling

**Files:**
- Modify: `bot.py`
- Test: `tests/test_p0_subdub_restore400_live_background_mp4.py`

- [x] Rehydrate persisted status after restart.
- [x] Deliver a validated existing MP4 once when no prior delivery attempt exists.
- [x] Never restart ASR, translation or TTS from status/recovery.
- [x] Terminalize an interrupted job without an MP4 as failed-no-charge instead of leaving 5/35/65/90% stale.

### Task 6: Verify and ship

**Files:**
- Modify only files listed above.

- [x] Run focused tests and restore400 regressions.
- [x] Run module compile, `bot.py` tokenize/compile truth and `git diff --check` (`bot.py py_compile`: TIMEOUT after 120 seconds; tokenize and selected-function AST compile PASS).
- [ ] Commit, push and open a reviewable PR.
- [ ] Merge/deploy only after gates pass.
- [ ] Coordinate exclusive Telegram access, then smoke one lane at a time and claim LIVE PASS only after four real processed MP4 deliveries with terminal 100% panels and reports.
